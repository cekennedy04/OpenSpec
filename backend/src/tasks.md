# Tasks: Stabilization

- [ ] Create OpenSpec change directory and documentation.
- [ ] Refactor `analysisService.ts` for Gemini stable `v1` and JSON mode.
- [ ] Update `index.ts` with enhanced CORS, logging, and port handling.
- [ ] Update `mobile.ts` to fix deprecated meta-tag warning.
- [ ] Verify backend health at `http://localhost:3000/api/health`.